# Retired Kind-fleet one-shots

These scripts were Phase-4 live surgery. Do not run them as SoT.

Ownership now:
- Edge / Traefik apps bridge / Vault seed / Keycloak admin → Terraform
- Application desired state → am-gitops PR + Argo sync
- Debug → parent `scripts/kind-fleet` (`_debug_*`, `_vault_*` read, `phase_gates`)

See `../README.md`.
